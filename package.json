{
  "name": "lords_of_the_edge",
  "version": "1.0.0",
  "description": "Entry for secret santa jam 2023.",
  "main": "index.js",
  "scripts": {
    "start:win": "cd dist && tsc && cleo.exe",
    "start:linux": "cd ./dist && tsc && ./cleo",
    "preinstall": "mkdir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
